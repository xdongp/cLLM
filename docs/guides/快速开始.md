# cLLM 快速开始

**5分钟快速上手 cLLM 项目！** 🚀

---

## 📋 前置条件

- C++17 或更高版本编译器
- CMake 3.15+
- Git

---

## 🚀 5分钟入门

### 第1步: 克隆项目 (30秒)

```bash
git clone <repository_url>
cd xllm/cpp/cLLM
```

### 第2步: 安装依赖 (5分钟)

```bash
# 查看详细安装指南
cat docs/guides/开发环境搭建.md

# 快速安装 (macOS)
brew install cmake libtorch nlohmann-json spdlog

# 快速安装 (Ubuntu)
sudo apt-get install cmake libtorch-dev nlohmann-json3-dev libspdlog-dev
```

### 第3步: 编译项目 (2分钟)

```bash
mkdir build && cd build
cmake ..
make -j$(nproc)
```

### 第4步: 运行测试 (1分钟)

```bash
# 运行 Tokenizer 测试
./test_tokenizer

# 运行基础测试
./test_basic
```

### 第5步: 启动服务 (30秒)

```bash
# 启动 HTTP 服务器
./cllm_server --config ../config/default.yaml

# 测试服务
curl http://localhost:8080/health
```

---

## ✅ 验证安装

### 检查编译结果

```bash
# 查看生成的可执行文件
ls -lh build/ | grep -E "(test_|cllm_)"

# 应该看到:
# test_tokenizer
# test_basic
# cllm_server
```

### 运行完整测试

```bash
cd build
ctest --output-on-failure
```

---

## 📚 下一步

### 深入学习

1. **了解架构**
   - [cLLM详细设计](../architecture/cLLM详细设计.md) - 完整架构
   - [组件交互设计](../architecture/组件交互设计.md) - 模块关系

2. **学习开发规范**
   - [C++编程规范](../specifications/C++编程规范_团队版.md) - 编码标准
   - [CodeBuddy使用指南](./CodeBuddy使用指南.md) - AI辅助开发

3. **查看模块设计**
   - [Tokenizer模块](../modules/Tokenizer模块设计.md)
   - [Scheduler模块](../modules/调度器模块设计.md)
   - [更多模块...](../modules/)

### 开始开发

```bash
# 创建开发分支
git checkout -b feature/your-feature

# 编写代码
# ... (遵守编程规范)

# 测试
make test

# 提交
git add .
git commit -m "feat: your feature description"
```

---

## 🐛 常见问题

### Q1: 编译失败?

**检查编译器版本**:
```bash
g++ --version  # 需要 GCC 7+ 或 Clang 5+
cmake --version  # 需要 CMake 3.15+
```

**检查依赖**:
```bash
# 查看缺失的库
ldd build/test_tokenizer
```

### Q2: 测试失败?

**查看测试日志**:
```bash
cd build
ctest --output-on-failure --verbose
```

**检查配置文件**:
```bash
cat ../config/default.yaml
# 确保路径正确
```

### Q3: 服务启动失败?

**检查端口占用**:
```bash
lsof -i :8080
```

**查看日志**:
```bash
tail -f logs/cllm_server.log
```

---

## 💡 开发技巧

### 使用 CodeBuddy AI

cLLM 项目配置了 CodeBuddy AI 助手:

```bash
# 查看 AI 约束规则
ls -la .codebuddy/rules/

# AI 会自动遵守:
# - C++17 标准
# - 命名规范
# - 架构约束
# - 代码质量标准
```

详见: [CodeBuddy使用指南](./CodeBuddy使用指南.md)

### 调试技巧

```bash
# 使用 GDB 调试
gdb ./test_tokenizer
break Tokenizer::encode
run

# 使用 Valgrind 检查内存
valgrind --leak-check=full ./test_tokenizer
```

详见: [调试技巧](./调试技巧.md)

---

## 📞 获取帮助

- **文档导航**: [00_文档导航.md](../00_文档导航.md)
- **开发指南**: [guides/](../guides/)
- **技术问题**: 查看 [issues](../../issues/)

---

## 🎉 恭喜！

您已经成功搭建 cLLM 开发环境！

**开始您的开发之旅吧！** 🚀

---

**更新日期**: 2026-01-11  
**维护者**: cLLM Core Team
