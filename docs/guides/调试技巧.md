# cLLM 调试技巧

**快速定位和解决问题** 🐛

---

## 🔍 GDB 调试

### 基础调试

```bash
# 编译 Debug 版本
cmake -DCMAKE_BUILD_TYPE=Debug ..
make

# 启动 GDB
gdb ./test_tokenizer

# 常用命令
break Tokenizer::encode  # 设置断点
run                       # 运行
next                      # 下一行
step                      # 进入函数
print variable            # 打印变量
backtrace                 # 查看调用栈
```

### 条件断点

```bash
break tokenizer.cpp:42 if token_id == 0
```

---

## 📊 性能分析

### 使用 Valgrind

```bash
# 检查内存泄漏
valgrind --leak-check=full ./test_tokenizer

# 性能分析
valgrind --tool=callgrind ./test_tokenizer
```

### 使用 gprof

```bash
# 编译时启用
cmake -DCMAKE_CXX_FLAGS="-pg" ..
make
./test_tokenizer
gprof test_tokenizer gprof.out
```

---

## 📝 日志调试

### 日志级别

```cpp
CLLM_DEBUG("详细调试信息");
CLLM_INFO("一般信息");
CLLM_WARN("警告信息");
CLLM_ERROR("错误信息");
```

### 配置日志

```yaml
# config/default.yaml
logging:
  level: debug
  file: logs/cllm.log
```

---

## 🔧 常见问题

### Q1: Segmentation Fault

**排查**:
```bash
gdb ./test_tokenizer
run
backtrace  # 查看崩溃位置
```

### Q2: 内存泄漏

**检查**:
```bash
valgrind --leak-check=full --show-leak-kinds=all ./test_tokenizer
```

### Q3: 性能问题

**分析**:
```bash
# CPU 占用
perf record ./test_tokenizer
perf report

# 内存占用
valgrind --tool=massif ./test_tokenizer
```

---

**更新日期**: 2026-01-11  
**维护者**: cLLM Core Team
