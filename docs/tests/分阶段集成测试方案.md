# cLLM 分阶段集成测试方案

**版本**: v1.0  
**创建日期**: 2026-01-11  
**目标**: 构建可并行化、解耦的分阶段集成测试体系  

---

## 📋 目录

1. [测试架构总览](#测试架构总览)
2. [测试阶段划分](#测试阶段划分)
3. [模块解耦策略](#模块解耦策略)
4. [并行化设计](#并行化设计)
5. [细粒度任务拆分](#细粒度任务拆分)
6. [Agent 分工方案](#agent-分工方案)
7. [测试数据准备](#测试数据准备)
8. [执行流程](#执行流程)

---

## 🏗️ 测试架构总览

### 核心模块

```
┌─────────────────────────────────────────────────────────────┐
│                     cLLM 集成测试架构                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────┐      ┌─────────────┐      ┌─────────────┐ │
│  │ HTTP Server │◄────►│  Tokenizer  │◄────►│   Model     │ │
│  │             │      │ (HFTokenizer)│      │  Executor   │ │
│  └─────────────┘      └─────────────┘      └─────────────┘ │
│         │                     │                     │        │
│         │                     │                     │        │
│         ▼                     ▼                     ▼        │
│  ┌─────────────────────────────────────────────────────┐   │
│  │            LibTorch Backend (推理后端)              │   │
│  └─────────────────────────────────────────────────────┘   │
│                           │                                  │
│                           ▼                                  │
│                  ┌─────────────────┐                        │
│                  │   Qwen3 Model   │                        │
│                  └─────────────────┘                        │
└─────────────────────────────────────────────────────────────┘
```

### 测试层次结构

```
Level 0: 单元测试 (Unit Tests)
    ├── HTTP Server 单元测试
    ├── HFTokenizer 单元测试
    ├── ModelExecutor 单元测试
    ├── LibTorch Backend 单元测试
    └── Qwen3 加载测试

Level 1: 模块集成测试 (Module Integration)
    ├── HTTP + Tokenizer 集成
    ├── Tokenizer + ModelExecutor 集成
    ├── ModelExecutor + LibTorch 集成
    └── LibTorch + Qwen3 集成

Level 2: 子系统集成测试 (Subsystem Integration)
    ├── 前端子系统 (HTTP + Tokenizer)
    ├── 推理子系统 (ModelExecutor + LibTorch)
    └── 端到端子系统 (Tokenizer → Executor → Backend)

Level 3: 系统集成测试 (System Integration)
    └── 完整流程 (HTTP → Tokenizer → Executor → LibTorch → Qwen3)

Level 4: 端到端测试 (E2E Tests)
    ├── 功能场景测试
    ├── 性能测试
    └── 压力测试
```

---

## 🎯 测试阶段划分

### 阶段 0: 准备阶段 (Phase 0 - Preparation)

**目标**: 准备测试环境和数据

**任务清单**:
- [ ] **P0.1**: 下载 Qwen3 模型
- [ ] **P0.2**: 准备测试数据集
- [ ] **P0.3**: 配置测试环境
- [ ] **P0.4**: 编译所有测试目标
- [ ] **P0.5**: 创建 Mock 对象和 Stub

**可并行**: ✅ 所有任务

**依赖**: 无

**输出**:
- Qwen3 模型文件（tokenizer.json, model weights）
- 测试数据集（JSON 文件）
- 编译产物（所有测试二进制）
- Mock/Stub 代码

---

### 阶段 1: 单元测试阶段 (Phase 1 - Unit Tests)

**目标**: 验证每个模块的独立功能

#### 1.1 HTTP Server 单元测试

**范围**:
- 路由注册和解析
- 请求/响应处理
- JSON 序列化/反序列化
- 错误处理

**测试任务**:
```cpp
// Task 1.1.1: 路由测试
test_http_route_registration()
test_http_route_matching()
test_http_method_filtering()

// Task 1.1.2: 请求处理测试
test_http_request_parsing()
test_http_query_params()
test_http_body_parsing()

// Task 1.1.3: 响应测试
test_http_response_building()
test_http_status_codes()
test_http_headers()

// Task 1.1.4: 错误处理测试
test_http_404_not_found()
test_http_500_internal_error()
test_http_timeout_handling()
```

**Mock 依赖**:
- Mock Tokenizer（返回假的 Token IDs）
- Mock ModelExecutor（返回假的推理结果）

**并行度**: 4 个 Agent 可并行

**预估时间**: 2 小时

---

#### 1.2 HFTokenizer 单元测试

**范围**:
- tokenizer.json 加载
- 文本编码/解码
- 特殊 Token 处理
- 批量处理

**测试任务**:
```cpp
// Task 1.2.1: 加载测试
test_hf_load_tokenizer()
test_hf_invalid_path()
test_hf_missing_config()

// Task 1.2.2: 编码测试
test_hf_encode_english()
test_hf_encode_chinese()
test_hf_encode_mixed()
test_hf_encode_special_chars()

// Task 1.2.3: 解码测试
test_hf_decode_tokens()
test_hf_decode_empty()
test_hf_decode_special_tokens()

// Task 1.2.4: 批量处理测试
test_hf_batch_encode()
test_hf_batch_decode()
test_hf_batch_performance()
```

**Mock 依赖**:
- 无（使用真实 tokenizers-cpp）

**并行度**: 4 个 Agent 可并行

**预估时间**: 2 小时

---

#### 1.3 ModelExecutor 单元测试

**范围**:
- 模型初始化
- 推理接口
- 批处理管理
- 状态管理

**测试任务**:
```cpp
// Task 1.3.1: 初始化测试
test_executor_init()
test_executor_load_config()
test_executor_invalid_config()

// Task 1.3.2: 推理接口测试
test_executor_forward()
test_executor_generate()
test_executor_batch_forward()

// Task 1.3.3: 状态管理测试
test_executor_state_reset()
test_executor_state_save()
test_executor_state_restore()

// Task 1.3.4: 错误处理测试
test_executor_invalid_input()
test_executor_memory_overflow()
test_executor_timeout()
```

**Mock 依赖**:
- Mock LibTorch Backend（返回假的 Tensor）
- Mock Tokenizer（提供 Token IDs）

**并行度**: 4 个 Agent 可并行

**预估时间**: 3 小时

---

#### 1.4 LibTorch Backend 单元测试

**范围**:
- Torch 模型加载
- Tensor 操作
- 前向推理
- 内存管理

**测试任务**:
```cpp
// Task 1.4.1: 模型加载测试
test_libtorch_load_model()
test_libtorch_invalid_model()
test_libtorch_model_format()

// Task 1.4.2: Tensor 操作测试
test_libtorch_tensor_creation()
test_libtorch_tensor_conversion()
test_libtorch_tensor_reshape()

// Task 1.4.3: 推理测试
test_libtorch_forward()
test_libtorch_batch_forward()
test_libtorch_dynamic_shape()

// Task 1.4.4: 内存测试
test_libtorch_memory_allocation()
test_libtorch_memory_release()
test_libtorch_cuda_memory()
```

**Mock 依赖**:
- 使用小型测试模型（非 Qwen3）

**并行度**: 4 个 Agent 可并行

**预估时间**: 3 小时

---

#### 1.5 Qwen3 模型单元测试

**范围**:
- 模型文件加载
- Tokenizer 兼容性
- 基本推理验证

**测试任务**:
```cpp
// Task 1.5.1: 模型加载测试
test_qwen3_load_model()
test_qwen3_config_parsing()
test_qwen3_weights_validation()

// Task 1.5.2: Tokenizer 测试
test_qwen3_tokenizer_load()
test_qwen3_special_tokens()
test_qwen3_vocab_size()

// Task 1.5.3: 基本推理测试
test_qwen3_single_forward()
test_qwen3_input_validation()
test_qwen3_output_shape()
```

**Mock 依赖**:
- 无（使用真实 Qwen3 模型）

**并行度**: 3 个 Agent 可并行

**预估时间**: 2 小时

---

### 阶段 2: 模块集成测试 (Phase 2 - Module Integration)

**目标**: 验证两个模块间的接口和数据流

#### 2.1 HTTP Server + HFTokenizer 集成

**测试场景**:
```
HTTP Request → JSON 解析 → 文本提取 → Tokenizer 编码 → 返回 Token IDs
```

**测试任务**:
```cpp
// Task 2.1.1: 端点集成测试
test_http_tokenize_endpoint()
test_http_detokenize_endpoint()

// Task 2.1.2: 数据流测试
test_http_to_tokenizer_data_flow()
test_tokenizer_to_http_response()

// Task 2.1.3: 错误传播测试
test_http_tokenizer_error_handling()
test_http_tokenizer_timeout()

// Task 2.1.4: 批量请求测试
test_http_batch_tokenize()
test_http_concurrent_requests()
```

**Mock 依赖**:
- Mock ModelExecutor（模拟推理结果）

**并行度**: 2 个 Agent（功能测试 + 性能测试）

**预估时间**: 3 小时

---

#### 2.2 HFTokenizer + ModelExecutor 集成

**测试场景**:
```
Token IDs → Tensor 转换 → ModelExecutor 推理 → 输出 Logits
```

**测试任务**:
```cpp
// Task 2.2.1: 数据格式转换测试
test_tokens_to_tensor()
test_tensor_shape_validation()

// Task 2.2.2: 推理流程测试
test_tokenizer_executor_forward()
test_tokenizer_executor_generate()

// Task 2.2.3: 批处理测试
test_tokenizer_executor_batch()
test_tokenizer_executor_dynamic_batch()

// Task 2.2.4: 状态同步测试
test_tokenizer_executor_state_sync()
test_tokenizer_executor_reset()
```

**Mock 依赖**:
- Mock LibTorch Backend（返回假的推理结果）

**并行度**: 2 个 Agent

**预估时间**: 3 小时

---

#### 2.3 ModelExecutor + LibTorch Backend 集成

**测试场景**:
```
Input Tensor → LibTorch 前向推理 → Output Tensor → 结果解析
```

**测试任务**:
```cpp
// Task 2.3.1: 推理流程测试
test_executor_backend_forward()
test_executor_backend_generate()

// Task 2.3.2: 内存管理测试
test_executor_backend_memory_sync()
test_executor_backend_cuda_transfer()

// Task 2.3.3: 性能测试
test_executor_backend_throughput()
test_executor_backend_latency()

// Task 2.3.4: 错误恢复测试
test_executor_backend_error_recovery()
test_executor_backend_timeout_handling()
```

**Mock 依赖**:
- 使用小型测试模型

**并行度**: 2 个 Agent

**预估时间**: 4 小时

---

#### 2.4 LibTorch Backend + Qwen3 Model 集成

**测试场景**:
```
加载 Qwen3 → 推理验证 → 输出质量检查
```

**测试任务**:
```cpp
// Task 2.4.1: 模型加载集成测试
test_backend_qwen3_load()
test_backend_qwen3_config_sync()

// Task 2.4.2: 推理正确性测试
test_backend_qwen3_forward()
test_backend_qwen3_output_validation()

// Task 2.4.3: 性能测试
test_backend_qwen3_throughput()
test_backend_qwen3_memory_usage()

// Task 2.4.4: 稳定性测试
test_backend_qwen3_long_sequence()
test_backend_qwen3_continuous_inference()
```

**Mock 依赖**:
- 无（使用真实模型）

**并行度**: 2 个 Agent

**预估时间**: 4 小时

---

### 阶段 3: 子系统集成测试 (Phase 3 - Subsystem Integration)

**目标**: 验证多个模块组合的功能

#### 3.1 前端子系统测试 (HTTP + Tokenizer)

**测试场景**:
```
完整的前端处理流程：
HTTP Request → 参数验证 → 文本预处理 → Tokenization → 返回结果
```

**测试任务**:
```cpp
// Task 3.1.1: 完整流程测试
test_frontend_complete_flow()
test_frontend_chat_endpoint()
test_frontend_completion_endpoint()

// Task 3.1.2: 并发测试
test_frontend_concurrent_requests()
test_frontend_load_balancing()

// Task 3.1.3: 性能测试
test_frontend_throughput()
test_frontend_latency()

// Task 3.1.4: 容错测试
test_frontend_error_recovery()
test_frontend_timeout_handling()
```

**Mock 依赖**:
- Mock ModelExecutor（模拟推理）

**并行度**: 2 个 Agent

**预估时间**: 4 小时

---

#### 3.2 推理子系统测试 (ModelExecutor + LibTorch + Qwen3)

**测试场景**:
```
完整的推理流程：
Token IDs → ModelExecutor → LibTorch → Qwen3 → 输出 Token IDs
```

**测试任务**:
```cpp
// Task 3.2.1: 完整推理流程测试
test_inference_complete_flow()
test_inference_generation()

// Task 3.2.2: 批处理测试
test_inference_batch_processing()
test_inference_dynamic_batching()

// Task 3.2.3: 性能测试
test_inference_throughput()
test_inference_memory_efficiency()

// Task 3.2.4: 质量测试
test_inference_output_quality()
test_inference_consistency()
```

**Mock 依赖**:
- Mock HTTP Server（提供输入）

**并行度**: 2 个 Agent

**预估时间**: 5 小时

---

#### 3.3 端到端子系统测试 (Tokenizer → Executor → Backend)

**测试场景**:
```
完整的推理链路（不含 HTTP）：
文本 → Tokenizer → Executor → LibTorch → 文本输出
```

**测试任务**:
```cpp
// Task 3.3.1: 完整链路测试
test_e2e_text_to_text()
test_e2e_chat_completion()

// Task 3.3.2: 流式输出测试
test_e2e_streaming()
test_e2e_streaming_latency()

// Task 3.3.3: 多轮对话测试
test_e2e_multi_turn_chat()
test_e2e_context_management()

// Task 3.3.4: 边界测试
test_e2e_long_input()
test_e2e_long_output()
test_e2e_special_characters()
```

**Mock 依赖**:
- 无（真实流程）

**并行度**: 2 个 Agent

**预估时间**: 5 小时

---

### 阶段 4: 系统集成测试 (Phase 4 - System Integration)

**目标**: 验证完整系统功能

#### 4.1 完整系统功能测试

**测试场景**:
```
完整的端到端流程：
HTTP Request → Tokenizer → Executor → LibTorch → Qwen3 → HTTP Response
```

**测试任务**:
```cpp
// Task 4.1.1: 核心功能测试
test_system_chat_completion()
test_system_text_completion()
test_system_streaming()

// Task 4.1.2: API 兼容性测试
test_system_openai_api()
test_system_custom_api()

// Task 4.1.3: 多场景测试
test_system_short_text()
test_system_long_text()
test_system_multi_turn()
test_system_concurrent_users()

// Task 4.1.4: 错误处理测试
test_system_error_propagation()
test_system_graceful_degradation()
```

**Mock 依赖**:
- 无（真实系统）

**并行度**: 3 个 Agent（功能 + 性能 + 稳定性）

**预估时间**: 6 小时

---

#### 4.2 性能基准测试

**测试维度**:
- 吞吐量（QPS）
- 延迟（P50, P95, P99）
- 资源使用（CPU, 内存, GPU）

**测试任务**:
```cpp
// Task 4.2.1: 吞吐量测试
test_benchmark_throughput_single()
test_benchmark_throughput_batch()
test_benchmark_throughput_concurrent()

// Task 4.2.2: 延迟测试
test_benchmark_latency_p50()
test_benchmark_latency_p95()
test_benchmark_latency_p99()

// Task 4.2.3: 资源测试
test_benchmark_cpu_usage()
test_benchmark_memory_usage()
test_benchmark_gpu_usage()

// Task 4.2.4: 扩展性测试
test_benchmark_scaling_users()
test_benchmark_scaling_batch_size()
```

**并行度**: 3 个 Agent（独立测试不同维度）

**预估时间**: 6 小时

---

#### 4.3 压力测试和稳定性测试

**测试场景**:
- 长时间运行
- 高并发
- 异常注入

**测试任务**:
```cpp
// Task 4.3.1: 压力测试
test_stress_high_concurrency()
test_stress_long_running()
test_stress_memory_pressure()

// Task 4.3.2: 稳定性测试
test_stability_continuous_load()
test_stability_spike_load()
test_stability_resource_exhaustion()

// Task 4.3.3: 异常注入测试
test_chaos_network_failure()
test_chaos_disk_failure()
test_chaos_oom_killer()

// Task 4.3.4: 恢复测试
test_recovery_crash_restart()
test_recovery_state_restore()
```

**并行度**: 2 个 Agent

**预估时间**: 8 小时

---

### 阶段 5: 端到端场景测试 (Phase 5 - E2E Scenarios)

**目标**: 验证真实使用场景

#### 5.1 真实场景测试

**测试场景**:
```
1. 单轮问答
2. 多轮对话
3. 代码生成
4. 文本摘要
5. 翻译任务
```

**测试任务**:
```cpp
// Task 5.1.1: 单轮问答测试
test_scenario_qa_factual()
test_scenario_qa_reasoning()
test_scenario_qa_math()

// Task 5.1.2: 多轮对话测试
test_scenario_chat_casual()
test_scenario_chat_technical()
test_scenario_chat_long_context()

// Task 5.1.3: 专业任务测试
test_scenario_code_generation()
test_scenario_text_summarization()
test_scenario_translation()

// Task 5.1.4: 质量评估
test_scenario_output_quality()
test_scenario_response_coherence()
```

**并行度**: 3 个 Agent（不同场景类型）

**预估时间**: 6 小时

---

## 🔧 模块解耦策略

### 1. 接口抽象

#### 1.1 Tokenizer 接口抽象

```cpp
// include/cllm/tokenizer/interface.h
class ITokenizer {
public:
    virtual ~ITokenizer() = default;
    
    // 核心接口
    virtual std::vector<int> encode(const std::string& text) = 0;
    virtual std::string decode(const std::vector<int>& ids) = 0;
    
    // 批量接口
    virtual std::vector<std::vector<int>> batchEncode(
        const std::vector<std::string>& texts) = 0;
    
    // 状态查询
    virtual bool isLoaded() const = 0;
    virtual int getVocabSize() const = 0;
};

// Mock 实现
class MockTokenizer : public ITokenizer {
private:
    std::unordered_map<std::string, std::vector<int>> mockResponses_;
    
public:
    void setMockResponse(const std::string& text, 
                         const std::vector<int>& ids) {
        mockResponses_[text] = ids;
    }
    
    std::vector<int> encode(const std::string& text) override {
        if (mockResponses_.count(text)) {
            return mockResponses_[text];
        }
        // 默认返回简单的假数据
        return {101, 102, 103};
    }
    
    std::string decode(const std::vector<int>& ids) override {
        return "mock_output";
    }
    
    bool isLoaded() const override { return true; }
    int getVocabSize() const override { return 10000; }
};
```

---

#### 1.2 ModelExecutor 接口抽象

```cpp
// include/cllm/model/interface.h
class IModelExecutor {
public:
    virtual ~IModelExecutor() = default;
    
    // 推理接口
    virtual torch::Tensor forward(const torch::Tensor& input) = 0;
    virtual std::vector<int> generate(
        const std::vector<int>& prompt,
        int maxLength) = 0;
    
    // 状态管理
    virtual void reset() = 0;
    virtual bool isReady() const = 0;
};

// Mock 实现
class MockModelExecutor : public IModelExecutor {
private:
    std::vector<int> mockOutput_ = {1, 2, 3, 4, 5};
    
public:
    void setMockOutput(const std::vector<int>& output) {
        mockOutput_ = output;
    }
    
    torch::Tensor forward(const torch::Tensor& input) override {
        // 返回假的 Tensor
        return torch::randn({1, 10, 32000});
    }
    
    std::vector<int> generate(
        const std::vector<int>& prompt,
        int maxLength) override {
        return mockOutput_;
    }
    
    void reset() override {}
    bool isReady() const override { return true; }
};
```

---

#### 1.3 Backend 接口抽象

```cpp
// include/cllm/inference/interface.h
class IInferenceBackend {
public:
    virtual ~IInferenceBackend() = default;
    
    // 模型加载
    virtual bool loadModel(const std::string& modelPath) = 0;
    
    // 推理接口
    virtual torch::Tensor forward(
        const torch::Tensor& input,
        const torch::Tensor& attention_mask) = 0;
    
    // 状态查询
    virtual bool isModelLoaded() const = 0;
    virtual std::string getModelType() const = 0;
};

// Mock 实现
class MockBackend : public IInferenceBackend {
private:
    bool loaded_ = false;
    
public:
    bool loadModel(const std::string& modelPath) override {
        loaded_ = true;
        return true;
    }
    
    torch::Tensor forward(
        const torch::Tensor& input,
        const torch::Tensor& attention_mask) override {
        auto batch_size = input.size(0);
        auto seq_len = input.size(1);
        // 返回假的 logits
        return torch::randn({batch_size, seq_len, 32000});
    }
    
    bool isModelLoaded() const override { return loaded_; }
    std::string getModelType() const override { return "mock"; }
};
```

---

#### 1.4 HTTP Server 接口抽象

```cpp
// include/cllm/server/interface.h
class IHTTPHandler {
public:
    virtual ~IHTTPHandler() = default;
    
    // 请求处理
    virtual nlohmann::json handleRequest(
        const nlohmann::json& request) = 0;
    
    // 路由注册
    virtual void registerRoute(
        const std::string& path,
        std::function<nlohmann::json(const nlohmann::json&)> handler) = 0;
};

// Mock 实现
class MockHTTPHandler : public IHTTPHandler {
private:
    std::map<std::string, nlohmann::json> mockResponses_;
    
public:
    void setMockResponse(const std::string& path,
                         const nlohmann::json& response) {
        mockResponses_[path] = response;
    }
    
    nlohmann::json handleRequest(
        const nlohmann::json& request) override {
        std::string path = request["path"];
        if (mockResponses_.count(path)) {
            return mockResponses_[path];
        }
        return {{"status", "ok"}};
    }
    
    void registerRoute(
        const std::string& path,
        std::function<nlohmann::json(const nlohmann::json&)> handler) override {
        // Mock 实现
    }
};
```

---

### 2. 依赖注入

#### 2.1 构造函数注入

```cpp
// 使用依赖注入实现解耦
class HTTPServer {
private:
    std::shared_ptr<ITokenizer> tokenizer_;
    std::shared_ptr<IModelExecutor> executor_;
    
public:
    // 构造函数注入依赖
    HTTPServer(std::shared_ptr<ITokenizer> tokenizer,
               std::shared_ptr<IModelExecutor> executor)
        : tokenizer_(tokenizer), executor_(executor) {}
    
    void handleChatRequest(const nlohmann::json& request) {
        // 使用注入的依赖
        auto text = request["messages"][0]["content"].get<std::string>();
        auto tokens = tokenizer_->encode(text);
        auto output = executor_->generate(tokens, 100);
        auto response = tokenizer_->decode(output);
        // ...
    }
};

// 测试中使用 Mock 对象
TEST(HTTPServerTest, ChatRequestWithMock) {
    // 创建 Mock 对象
    auto mockTokenizer = std::make_shared<MockTokenizer>();
    auto mockExecutor = std::make_shared<MockModelExecutor>();
    
    // 设置 Mock 行为
    mockTokenizer->setMockResponse("Hello", {1, 2, 3});
    mockExecutor->setMockOutput({4, 5, 6});
    
    // 注入 Mock 对象
    HTTPServer server(mockTokenizer, mockExecutor);
    
    // 测试
    nlohmann::json request = {
        {"messages", {{{"role", "user"}, {"content", "Hello"}}}}
    };
    server.handleChatRequest(request);
    
    // 验证
    // ...
}
```

---

### 3. 测试隔离策略

#### 3.1 使用 Test Fixture

```cpp
// tests/fixtures/tokenizer_test_fixture.h
class TokenizerTestFixture : public ::testing::Test {
protected:
    std::shared_ptr<ITokenizer> tokenizer_;
    std::string testModelPath_;
    
    void SetUp() override {
        testModelPath_ = std::getenv("CLLM_TEST_MODEL_PATH");
        if (testModelPath_.empty()) {
            // 使用 Mock
            tokenizer_ = std::make_shared<MockTokenizer>();
        } else {
            // 使用真实 Tokenizer
            tokenizer_ = std::make_shared<HFTokenizer>();
            tokenizer_->load(testModelPath_);
        }
    }
    
    void TearDown() override {
        tokenizer_.reset();
    }
};

// 使用 Fixture
TEST_F(TokenizerTestFixture, EncodeTest) {
    auto ids = tokenizer_->encode("Hello, world!");
    EXPECT_FALSE(ids.empty());
}
```

---

#### 3.2 测试数据隔离

```cpp
// tests/data/test_data_manager.h
class TestDataManager {
public:
    static TestDataManager& getInstance() {
        static TestDataManager instance;
        return instance;
    }
    
    // 获取测试数据
    std::vector<std::string> getTestTexts(const std::string& category) {
        if (category == "short") {
            return {"Hello", "Hi", "Test"};
        } else if (category == "long") {
            return {/* long texts */};
        }
        return {};
    }
    
    // 获取预期结果
    std::vector<int> getExpectedTokens(const std::string& text) {
        // 从文件加载或硬编码
        return expectedResults_[text];
    }
    
private:
    std::map<std::string, std::vector<int>> expectedResults_;
    
    TestDataManager() {
        loadTestData();
    }
    
    void loadTestData() {
        // 从 JSON 文件加载测试数据
        std::ifstream f("tests/data/test_cases.json");
        nlohmann::json data = nlohmann::json::parse(f);
        // ...
    }
};
```

---

## 🚀 并行化设计

### 1. 任务依赖图

```
Phase 0 (准备)
    ├── P0.1 (下载模型)
    ├── P0.2 (准备数据)
    ├── P0.3 (配置环境)
    ├── P0.4 (编译测试)
    └── P0.5 (创建Mock)
            │
            ▼
    ┌───────┴───────┬───────────┬──────────┐
    │               │           │          │
Phase 1.1       Phase 1.2   Phase 1.3  Phase 1.4
(HTTP单元)      (Tokenizer) (Executor) (Backend)
    │               │           │          │
    └───────┬───────┴───────┬───┴──────────┘
            │               │
            ▼               ▼
        Phase 2.1       Phase 2.2
     (HTTP+Tokenizer) (Tokenizer+Executor)
            │               │
            └───────┬───────┘
                    │
                    ▼
                Phase 3.1
             (前端子系统)
                    │
                    ▼
                Phase 4.1
              (系统集成)
                    │
                    ▼
                Phase 5.1
             (E2E场景)
```

---

### 2. 并行执行策略

#### 2.1 阶段内并行

**Phase 1 单元测试 - 完全并行**:
```bash
# 5 个模块可以完全并行测试
Agent-1: test_http_server_unit
Agent-2: test_hf_tokenizer_unit
Agent-3: test_model_executor_unit
Agent-4: test_libtorch_backend_unit
Agent-5: test_qwen3_model_unit

# 执行
parallel ::: \
  "./build/bin/test_http_server_unit" \
  "./build/bin/test_hf_tokenizer_unit" \
  "./build/bin/test_model_executor_unit" \
  "./build/bin/test_libtorch_backend_unit" \
  "./build/bin/test_qwen3_model_unit"
```

**Phase 2 模块集成 - 部分并行**:
```bash
# 2.1 和 2.3 可以并行（无依赖）
Agent-1: test_http_tokenizer_integration
Agent-2: test_executor_backend_integration

# 2.2 和 2.4 可以并行
Agent-3: test_tokenizer_executor_integration
Agent-4: test_backend_qwen3_integration
```

---

#### 2.2 跨阶段并行

**同时运行不同阶段的独立任务**:
```bash
# 当 Phase 1.1 和 1.2 完成后
# 可以启动 Phase 2.1 (HTTP + Tokenizer)
# 同时 Phase 1.3, 1.4 还在运行

# 使用任务队列管理
TaskQueue:
  [Running] Phase 1.3 (Executor Unit)
  [Running] Phase 1.4 (Backend Unit)
  [Running] Phase 2.1 (HTTP + Tokenizer) ← 提前启动
  [Pending] Phase 2.2 (Tokenizer + Executor)
  [Pending] Phase 2.3 (Executor + Backend)
```

---

#### 2.3 测试并行化工具

**使用 GoogleTest 并行执行**:
```cpp
// tests/parallel_runner.cpp
#include <gtest/gtest.h>
#include <thread>
#include <vector>

class ParallelTestRunner {
public:
    void runTestSuite(const std::string& filter) {
        std::vector<std::thread> threads;
        
        // 分割测试用例
        auto testCases = splitTestCases(filter);
        
        // 并行执行
        for (const auto& testCase : testCases) {
            threads.emplace_back([testCase]() {
                testing::GTEST_FLAG(filter) = testCase;
                testing::InitGoogleTest();
                RUN_ALL_TESTS();
            });
        }
        
        // 等待完成
        for (auto& t : threads) {
            t.join();
        }
    }
    
private:
    std::vector<std::string> splitTestCases(const std::string& filter) {
        // 解析测试过滤器，分割成独立的测试用例
        return {"Test1.*", "Test2.*", "Test3.*"};
    }
};
```

**使用脚本并行执行**:
```bash
#!/bin/bash
# scripts/parallel_test.sh

# 定义测试列表
tests=(
    "test_http_server_unit"
    "test_hf_tokenizer_unit"
    "test_model_executor_unit"
    "test_libtorch_backend_unit"
)

# 并行执行函数
run_test() {
    local test_name=$1
    echo "Running $test_name..."
    ./build/bin/$test_name > logs/${test_name}.log 2>&1
    if [ $? -eq 0 ]; then
        echo "✅ $test_name PASSED"
    else
        echo "❌ $test_name FAILED"
    fi
}

# 导出函数供 parallel 使用
export -f run_test

# 使用 GNU parallel 并行执行
parallel -j 4 run_test ::: "${tests[@]}"

# 或使用 xargs
printf '%s\n' "${tests[@]}" | xargs -P 4 -I {} bash -c 'run_test "$@"' _ {}
```

---

### 3. Agent 分工策略

#### 3.1 按模块分工

```
Agent-1: HTTP Server 专家
  - 负责所有 HTTP Server 相关测试
  - Phase 1.1, 2.1, 3.1 的 HTTP 部分
  
Agent-2: Tokenizer 专家
  - 负责所有 Tokenizer 相关测试
  - Phase 1.2, 2.1, 2.2 的 Tokenizer 部分
  
Agent-3: ModelExecutor 专家
  - 负责所有 ModelExecutor 相关测试
  - Phase 1.3, 2.2, 2.3 的 Executor 部分
  
Agent-4: Backend 专家
  - 负责 LibTorch Backend 和 Qwen3 测试
  - Phase 1.4, 1.5, 2.3, 2.4 的 Backend 部分
  
Agent-5: 集成测试专家
  - 负责跨模块集成测试
  - Phase 3, 4, 5 的协调和执行
```

---

#### 3.2 按测试类型分工

```
Agent-Functional: 功能测试
  - 所有功能正确性测试
  - 覆盖所有阶段的功能用例
  
Agent-Performance: 性能测试
  - 所有性能基准测试
  - Phase 4.2 的所有性能测试
  
Agent-Stability: 稳定性测试
  - 压力测试和长时间运行测试
  - Phase 4.3 的所有稳定性测试
  
Agent-E2E: 端到端测试
  - 真实场景测试
  - Phase 5 的所有场景测试
```

---

#### 3.3 混合分工模式（推荐）

```
Phase 1 (单元测试):
  Agent-1: HTTP + Tokenizer 单元测试
  Agent-2: ModelExecutor + Backend 单元测试
  Agent-3: Qwen3 + Mock 创建

Phase 2 (模块集成):
  Agent-1: HTTP + Tokenizer 集成
  Agent-2: Tokenizer + Executor 集成
  Agent-3: Executor + Backend + Qwen3 集成

Phase 3 (子系统):
  Agent-1: 前端子系统测试
  Agent-2: 推理子系统测试
  Agent-3: E2E 子系统测试

Phase 4 (系统集成):
  Agent-1: 功能测试
  Agent-2: 性能测试
  Agent-3: 稳定性测试

Phase 5 (E2E 场景):
  Agent-1: 对话场景
  Agent-2: 专业任务场景
  Agent-3: 质量评估
```

---

## 📦 细粒度任务拆分

### 任务元数据结构

```json
{
  "task_id": "P1.1.1",
  "phase": 1,
  "module": "http_server",
  "type": "unit_test",
  "name": "HTTP路由注册测试",
  "description": "测试HTTP路由注册和匹配功能",
  "dependencies": ["P0.4"],
  "estimated_time": "30min",
  "priority": "high",
  "assignee": "Agent-1",
  "parallel_with": ["P1.2.1", "P1.3.1"],
  "test_file": "tests/test_http_route.cpp",
  "mock_requirements": ["MockTokenizer", "MockExecutor"],
  "acceptance_criteria": [
    "所有路由正确注册",
    "路由匹配准确",
    "错误路由返回404"
  ]
}
```

---

### 完整任务清单

#### Phase 0: 准备阶段 (5 个任务)

```json
[
  {
    "task_id": "P0.1",
    "name": "下载Qwen3模型",
    "agent": "Agent-Ops",
    "time": "60min",
    "parallel": true,
    "script": "scripts/download_qwen3.sh"
  },
  {
    "task_id": "P0.2",
    "name": "准备测试数据集",
    "agent": "Agent-Data",
    "time": "30min",
    "parallel": true,
    "script": "scripts/prepare_test_data.py"
  },
  {
    "task_id": "P0.3",
    "name": "配置测试环境",
    "agent": "Agent-Ops",
    "time": "15min",
    "parallel": true,
    "script": "scripts/setup_test_env.sh"
  },
  {
    "task_id": "P0.4",
    "name": "编译所有测试",
    "agent": "Agent-Build",
    "time": "20min",
    "parallel": false,
    "command": "make -j8 all_tests"
  },
  {
    "task_id": "P0.5",
    "name": "创建Mock对象",
    "agent": "Agent-1",
    "time": "45min",
    "parallel": false,
    "files": [
      "tests/mocks/mock_tokenizer.h",
      "tests/mocks/mock_executor.h",
      "tests/mocks/mock_backend.h"
    ]
  }
]
```

---

#### Phase 1: 单元测试 (20 个任务)

**HTTP Server (4 个任务)**:
```json
[
  {"task_id": "P1.1.1", "name": "路由测试", "time": "30min"},
  {"task_id": "P1.1.2", "name": "请求处理测试", "time": "30min"},
  {"task_id": "P1.1.3", "name": "响应测试", "time": "30min"},
  {"task_id": "P1.1.4", "name": "错误处理测试", "time": "30min"}
]
```

**HFTokenizer (4 个任务)**:
```json
[
  {"task_id": "P1.2.1", "name": "加载测试", "time": "30min"},
  {"task_id": "P1.2.2", "name": "编码测试", "time": "30min"},
  {"task_id": "P1.2.3", "name": "解码测试", "time": "30min"},
  {"task_id": "P1.2.4", "name": "批量处理测试", "time": "30min"}
]
```

**ModelExecutor (4 个任务)**:
```json
[
  {"task_id": "P1.3.1", "name": "初始化测试", "time": "45min"},
  {"task_id": "P1.3.2", "name": "推理接口测试", "time": "45min"},
  {"task_id": "P1.3.3", "name": "状态管理测试", "time": "45min"},
  {"task_id": "P1.3.4", "name": "错误处理测试", "time": "45min"}
]
```

**LibTorch Backend (4 个任务)**:
```json
[
  {"task_id": "P1.4.1", "name": "模型加载测试", "time": "45min"},
  {"task_id": "P1.4.2", "name": "Tensor操作测试", "time": "45min"},
  {"task_id": "P1.4.3", "name": "推理测试", "time": "45min"},
  {"task_id": "P1.4.4", "name": "内存测试", "time": "45min"}
]
```

**Qwen3 Model (3 个任务)**:
```json
[
  {"task_id": "P1.5.1", "name": "模型加载测试", "time": "30min"},
  {"task_id": "P1.5.2", "name": "Tokenizer测试", "time": "30min"},
  {"task_id": "P1.5.3", "name": "基本推理测试", "time": "60min"}
]
```

---

#### Phase 2: 模块集成测试 (16 个任务)

```json
[
  {
    "task_id": "P2.1.1",
    "name": "HTTP+Tokenizer端点集成",
    "time": "45min",
    "dependencies": ["P1.1.4", "P1.2.4"]
  },
  {
    "task_id": "P2.1.2",
    "name": "HTTP+Tokenizer数据流",
    "time": "45min"
  },
  {
    "task_id": "P2.1.3",
    "name": "HTTP+Tokenizer错误传播",
    "time": "45min"
  },
  {
    "task_id": "P2.1.4",
    "name": "HTTP+Tokenizer批量请求",
    "time": "45min"
  },
  
  {
    "task_id": "P2.2.1",
    "name": "Tokenizer+Executor数据转换",
    "time": "45min",
    "dependencies": ["P1.2.4", "P1.3.4"]
  },
  {
    "task_id": "P2.2.2",
    "name": "Tokenizer+Executor推理流程",
    "time": "45min"
  },
  {
    "task_id": "P2.2.3",
    "name": "Tokenizer+Executor批处理",
    "time": "45min"
  },
  {
    "task_id": "P2.2.4",
    "name": "Tokenizer+Executor状态同步",
    "time": "45min"
  },
  
  {
    "task_id": "P2.3.1",
    "name": "Executor+Backend推理流程",
    "time": "60min",
    "dependencies": ["P1.3.4", "P1.4.4"]
  },
  {
    "task_id": "P2.3.2",
    "name": "Executor+Backend内存管理",
    "time": "60min"
  },
  {
    "task_id": "P2.3.3",
    "name": "Executor+Backend性能测试",
    "time": "60min"
  },
  {
    "task_id": "P2.3.4",
    "name": "Executor+Backend错误恢复",
    "time": "60min"
  },
  
  {
    "task_id": "P2.4.1",
    "name": "Backend+Qwen3模型加载",
    "time": "60min",
    "dependencies": ["P1.4.4", "P1.5.3"]
  },
  {
    "task_id": "P2.4.2",
    "name": "Backend+Qwen3推理正确性",
    "time": "60min"
  },
  {
    "task_id": "P2.4.3",
    "name": "Backend+Qwen3性能测试",
    "time": "60min"
  },
  {
    "task_id": "P2.4.4",
    "name": "Backend+Qwen3稳定性测试",
    "time": "60min"
  }
]
```

---

#### Phase 3: 子系统测试 (12 个任务)

```json
[
  {
    "task_id": "P3.1.1",
    "name": "前端子系统完整流程",
    "time": "60min",
    "dependencies": ["P2.1.4"]
  },
  {
    "task_id": "P3.1.2",
    "name": "前端子系统并发测试",
    "time": "60min"
  },
  {
    "task_id": "P3.1.3",
    "name": "前端子系统性能测试",
    "time": "60min"
  },
  {
    "task_id": "P3.1.4",
    "name": "前端子系统容错测试",
    "time": "60min"
  },
  
  {
    "task_id": "P3.2.1",
    "name": "推理子系统完整流程",
    "time": "75min",
    "dependencies": ["P2.2.4", "P2.3.4", "P2.4.4"]
  },
  {
    "task_id": "P3.2.2",
    "name": "推理子系统批处理",
    "time": "75min"
  },
  {
    "task_id": "P3.2.3",
    "name": "推理子系统性能测试",
    "time": "75min"
  },
  {
    "task_id": "P3.2.4",
    "name": "推理子系统质量测试",
    "time": "75min"
  },
  
  {
    "task_id": "P3.3.1",
    "name": "E2E子系统完整链路",
    "time": "75min",
    "dependencies": ["P2.2.4", "P2.3.4"]
  },
  {
    "task_id": "P3.3.2",
    "name": "E2E子系统流式输出",
    "time": "75min"
  },
  {
    "task_id": "P3.3.3",
    "name": "E2E子系统多轮对话",
    "time": "75min"
  },
  {
    "task_id": "P3.3.4",
    "name": "E2E子系统边界测试",
    "time": "75min"
  }
]
```

---

#### Phase 4: 系统集成测试 (16 个任务)

```json
[
  {
    "task_id": "P4.1.1",
    "name": "系统核心功能测试",
    "time": "90min",
    "dependencies": ["P3.1.4", "P3.2.4", "P3.3.4"]
  },
  {
    "task_id": "P4.1.2",
    "name": "系统API兼容性测试",
    "time": "90min"
  },
  {
    "task_id": "P4.1.3",
    "name": "系统多场景测试",
    "time": "90min"
  },
  {
    "task_id": "P4.1.4",
    "name": "系统错误处理测试",
    "time": "90min"
  },
  
  {
    "task_id": "P4.2.1",
    "name": "性能基准-吞吐量",
    "time": "90min",
    "dependencies": ["P4.1.4"]
  },
  {
    "task_id": "P4.2.2",
    "name": "性能基准-延迟",
    "time": "90min"
  },
  {
    "task_id": "P4.2.3",
    "name": "性能基准-资源使用",
    "time": "90min"
  },
  {
    "task_id": "P4.2.4",
    "name": "性能基准-扩展性",
    "time": "90min"
  },
  
  {
    "task_id": "P4.3.1",
    "name": "压力测试",
    "time": "120min",
    "dependencies": ["P4.1.4"]
  },
  {
    "task_id": "P4.3.2",
    "name": "稳定性测试",
    "time": "120min"
  },
  {
    "task_id": "P4.3.3",
    "name": "异常注入测试",
    "time": "120min"
  },
  {
    "task_id": "P4.3.4",
    "name": "恢复测试",
    "time": "120min"
  }
]
```

---

#### Phase 5: E2E 场景测试 (12 个任务)

```json
[
  {
    "task_id": "P5.1.1",
    "name": "单轮问答场景",
    "time": "90min",
    "dependencies": ["P4.1.4"]
  },
  {
    "task_id": "P5.1.2",
    "name": "多轮对话场景",
    "time": "90min"
  },
  {
    "task_id": "P5.1.3",
    "name": "专业任务场景",
    "time": "90min"
  },
  {
    "task_id": "P5.1.4",
    "name": "质量评估",
    "time": "90min"
  }
]
```

---

## 👥 Agent 分工方案

### 分工表

| Agent ID | 角色 | 主要职责 | Phase 分工 | 预估总工时 |
|----------|------|---------|-----------|-----------|
| **Agent-1** | HTTP 专家 | HTTP Server 相关 | P1.1, P2.1, P3.1 | 10h |
| **Agent-2** | Tokenizer 专家 | HFTokenizer 相关 | P1.2, P2.1, P2.2 | 8h |
| **Agent-3** | Executor 专家 | ModelExecutor 相关 | P1.3, P2.2, P2.3 | 10h |
| **Agent-4** | Backend 专家 | LibTorch + Qwen3 | P1.4, P1.5, P2.3, P2.4 | 12h |
| **Agent-5** | 集成专家 | 子系统集成 | P3.2, P3.3 | 10h |
| **Agent-6** | 功能测试 | 系统功能测试 | P4.1, P5.1 | 12h |
| **Agent-7** | 性能测试 | 性能和压力测试 | P4.2, P4.3 | 14h |

---

### 详细分工

#### Agent-1: HTTP Server 专家

**职责**:
- HTTP Server 单元测试
- HTTP + Tokenizer 集成测试
- 前端子系统测试

**任务列表**:
```
Phase 1.1 - HTTP Server 单元测试 (2h)
  ✓ P1.1.1: 路由测试 (30min)
  ✓ P1.1.2: 请求处理测试 (30min)
  ✓ P1.1.3: 响应测试 (30min)
  ✓ P1.1.4: 错误处理测试 (30min)

Phase 2.1 - HTTP + Tokenizer 集成 (3h)
  ✓ P2.1.1: 端点集成测试 (45min)
  ✓ P2.1.2: 数据流测试 (45min)
  ✓ P2.1.3: 错误传播测试 (45min)
  ✓ P2.1.4: 批量请求测试 (45min)

Phase 3.1 - 前端子系统 (4h)
  ✓ P3.1.1: 完整流程测试 (60min)
  ✓ P3.1.2: 并发测试 (60min)
  ✓ P3.1.3: 性能测试 (60min)
  ✓ P3.1.4: 容错测试 (60min)

协作任务:
  - 与 Agent-2 协作完成 Phase 2.1
  - 提供 Mock HTTP Server 给其他 Agent
```

**技能要求**:
- 熟悉 HTTP 协议
- 熟悉 REST API 设计
- 了解并发处理

---

#### Agent-2: Tokenizer 专家

**职责**:
- HFTokenizer 单元测试
- Tokenizer 相关集成测试

**任务列表**:
```
Phase 1.2 - HFTokenizer 单元测试 (2h)
  ✓ P1.2.1: 加载测试 (30min)
  ✓ P1.2.2: 编码测试 (30min)
  ✓ P1.2.3: 解码测试 (30min)
  ✓ P1.2.4: 批量处理测试 (30min)

Phase 2.1 - HTTP + Tokenizer 集成 (3h)
  ✓ 协助 Agent-1 完成集成测试

Phase 2.2 - Tokenizer + Executor 集成 (3h)
  ✓ P2.2.1: 数据转换测试 (45min)
  ✓ P2.2.2: 推理流程测试 (45min)
  ✓ P2.2.3: 批处理测试 (45min)
  ✓ P2.2.4: 状态同步测试 (45min)

协作任务:
  - 与 Agent-1 协作完成 Phase 2.1
  - 与 Agent-3 协作完成 Phase 2.2
  - 提供 Mock Tokenizer 给其他 Agent
```

**技能要求**:
- 熟悉 tokenizers-cpp 库
- 了解 NLP 基础
- 熟悉字符编码

---

#### Agent-3: ModelExecutor 专家

**职责**:
- ModelExecutor 单元测试
- Executor 相关集成测试

**任务列表**:
```
Phase 1.3 - ModelExecutor 单元测试 (3h)
  ✓ P1.3.1: 初始化测试 (45min)
  ✓ P1.3.2: 推理接口测试 (45min)
  ✓ P1.3.3: 状态管理测试 (45min)
  ✓ P1.3.4: 错误处理测试 (45min)

Phase 2.2 - Tokenizer + Executor 集成 (3h)
  ✓ 协助 Agent-2 完成集成测试

Phase 2.3 - Executor + Backend 集成 (4h)
  ✓ P2.3.1: 推理流程测试 (60min)
  ✓ P2.3.2: 内存管理测试 (60min)
  ✓ P2.3.3: 性能测试 (60min)
  ✓ P2.3.4: 错误恢复测试 (60min)

协作任务:
  - 与 Agent-2 协作完成 Phase 2.2
  - 与 Agent-4 协作完成 Phase 2.3
  - 提供 Mock Executor 给其他 Agent
```

**技能要求**:
- 熟悉模型推理流程
- 了解批处理机制
- 熟悉状态管理

---

#### Agent-4: Backend & Model 专家

**职责**:
- LibTorch Backend 单元测试
- Qwen3 模型单元测试
- Backend 相关集成测试

**任务列表**:
```
Phase 1.4 - LibTorch Backend 单元测试 (3h)
  ✓ P1.4.1: 模型加载测试 (45min)
  ✓ P1.4.2: Tensor操作测试 (45min)
  ✓ P1.4.3: 推理测试 (45min)
  ✓ P1.4.4: 内存测试 (45min)

Phase 1.5 - Qwen3 Model 单元测试 (2h)
  ✓ P1.5.1: 模型加载测试 (30min)
  ✓ P1.5.2: Tokenizer测试 (30min)
  ✓ P1.5.3: 基本推理测试 (60min)

Phase 2.3 - Executor + Backend 集成 (4h)
  ✓ 协助 Agent-3 完成集成测试

Phase 2.4 - Backend + Qwen3 集成 (4h)
  ✓ P2.4.1: 模型加载测试 (60min)
  ✓ P2.4.2: 推理正确性测试 (60min)
  ✓ P2.4.3: 性能测试 (60min)
  ✓ P2.4.4: 稳定性测试 (60min)

协作任务:
  - 与 Agent-3 协作完成 Phase 2.3
  - 提供 Mock Backend 给其他 Agent
```

**技能要求**:
- 熟悉 LibTorch/PyTorch
- 了解深度学习推理
- 熟悉 GPU 编程（可选）

---

#### Agent-5: 集成测试专家

**职责**:
- 子系统集成测试
- 端到端测试协调

**任务列表**:
```
Phase 3.2 - 推理子系统测试 (5h)
  ✓ P3.2.1: 完整流程测试 (75min)
  ✓ P3.2.2: 批处理测试 (75min)
  ✓ P3.2.3: 性能测试 (75min)
  ✓ P3.2.4: 质量测试 (75min)

Phase 3.3 - E2E 子系统测试 (5h)
  ✓ P3.3.1: 完整链路测试 (75min)
  ✓ P3.3.2: 流式输出测试 (75min)
  ✓ P3.3.3: 多轮对话测试 (75min)
  ✓ P3.3.4: 边界测试 (75min)

协作任务:
  - 协调所有 Agent 的集成工作
  - 管理测试依赖关系
```

**技能要求**:
- 全栈测试经验
- 项目管理能力
- 问题诊断能力

---

#### Agent-6: 功能测试专家

**职责**:
- 系统功能测试
- E2E 场景测试

**任务列表**:
```
Phase 4.1 - 系统功能测试 (6h)
  ✓ P4.1.1: 核心功能测试 (90min)
  ✓ P4.1.2: API兼容性测试 (90min)
  ✓ P4.1.3: 多场景测试 (90min)
  ✓ P4.1.4: 错误处理测试 (90min)

Phase 5.1 - E2E 场景测试 (6h)
  ✓ P5.1.1: 单轮问答场景 (90min)
  ✓ P5.1.2: 多轮对话场景 (90min)
  ✓ P5.1.3: 专业任务场景 (90min)
  ✓ P5.1.4: 质量评估 (90min)

协作任务:
  - 与 Agent-5 协调系统测试
  - 收集用户反馈
```

**技能要求**:
- 功能测试经验
- 场景设计能力
- 质量评估能力

---

#### Agent-7: 性能测试专家

**职责**:
- 性能基准测试
- 压力测试和稳定性测试

**任务列表**:
```
Phase 4.2 - 性能基准测试 (6h)
  ✓ P4.2.1: 吞吐量测试 (90min)
  ✓ P4.2.2: 延迟测试 (90min)
  ✓ P4.2.3: 资源使用测试 (90min)
  ✓ P4.2.4: 扩展性测试 (90min)

Phase 4.3 - 压力和稳定性测试 (8h)
  ✓ P4.3.1: 压力测试 (120min)
  ✓ P4.3.2: 稳定性测试 (120min)
  ✓ P4.3.3: 异常注入测试 (120min)
  ✓ P4.3.4: 恢复测试 (120min)

协作任务:
  - 提供性能基准数据
  - 分析性能瓶颈
```

**技能要求**:
- 性能测试经验
- 压力测试工具使用
- 性能分析能力

---

## 📊 测试数据准备

### 测试数据结构

```json
{
  "test_data": {
    "tokenizer": {
      "short_texts": [
        "Hello, world!",
        "你好，世界！",
        "Test input"
      ],
      "long_texts": [
        "This is a very long text for testing...",
        "这是一个很长的中文测试文本..."
      ],
      "special_cases": [
        "😀🎉",
        "Text with\nnewlines",
        "Mixed中英文text"
      ],
      "expected_results": {
        "Hello, world!": {
          "token_ids": [101, 7592, 1010, 2088, 999, 102],
          "decoded": "Hello, world!"
        }
      }
    },
    "inference": {
      "prompts": [
        {
          "text": "What is artificial intelligence?",
          "max_length": 50,
          "expected_keywords": ["AI", "machine", "learning"]
        },
        {
          "text": "请介绍一下人工智能",
          "max_length": 100,
          "expected_keywords": ["人工智能", "机器学习"]
        }
      ]
    },
    "performance": {
      "batch_sizes": [1, 4, 8, 16, 32],
      "sequence_lengths": [10, 50, 100, 500, 1000],
      "concurrency_levels": [1, 5, 10, 20, 50]
    },
    "stress": {
      "duration_minutes": [5, 15, 30, 60],
      "request_rates": [10, 50, 100, 200],
      "payload_sizes": [100, 1000, 10000]
    }
  }
}
```

### 测试数据生成脚本

```python
# scripts/generate_test_data.py
import json
import random
import string

class TestDataGenerator:
    def __init__(self):
        self.data = {
            "tokenizer": {},
            "inference": {},
            "performance": {},
            "stress": {}
        }
    
    def generate_tokenizer_data(self):
        """生成 Tokenizer 测试数据"""
        # 短文本
        short_texts = [
            "Hello, world!",
            "你好，世界！",
            "Test input",
            "Simple test",
            "こんにちは"
        ]
        
        # 长文本
        long_texts = []
        for i in range(5):
            text = " ".join([
                "This is a long text for testing."
            ] * 50)
            long_texts.append(text)
        
        # 特殊情况
        special_cases = [
            "😀🎉🚀",
            "Text with\nnewlines\nand\ttabs",
            "Mixed中英文日本語text",
            "Special chars: !@#$%^&*()",
            "Numbers: 0123456789"
        ]
        
        self.data["tokenizer"] = {
            "short_texts": short_texts,
            "long_texts": long_texts,
            "special_cases": special_cases
        }
    
    def generate_inference_data(self):
        """生成推理测试数据"""
        prompts = [
            {
                "text": "What is artificial intelligence?",
                "max_length": 50,
                "temperature": 0.7,
                "expected_keywords": ["AI", "machine", "learning"]
            },
            {
                "text": "请介绍一下人工智能",
                "max_length": 100,
                "temperature": 0.7,
                "expected_keywords": ["人工智能", "机器学习", "深度学习"]
            },
            {
                "text": "Write a Python function to calculate fibonacci",
                "max_length": 200,
                "temperature": 0.3,
                "expected_keywords": ["def", "fibonacci", "return"]
            }
        ]
        
        self.data["inference"]["prompts"] = prompts
    
    def generate_performance_data(self):
        """生成性能测试数据"""
        self.data["performance"] = {
            "batch_sizes": [1, 2, 4, 8, 16, 32],
            "sequence_lengths": [10, 50, 100, 200, 500, 1000],
            "concurrency_levels": [1, 5, 10, 20, 50, 100]
        }
    
    def generate_stress_data(self):
        """生成压力测试数据"""
        self.data["stress"] = {
            "duration_minutes": [5, 15, 30, 60],
            "request_rates": [10, 50, 100, 200, 500],
            "payload_sizes": [100, 500, 1000, 5000, 10000]
        }
    
    def save(self, filepath):
        """保存测试数据"""
        with open(filepath, 'w', encoding='utf-8') as f:
            json.dump(self.data, f, ensure_ascii=False, indent=2)
    
    def generate_all(self):
        """生成所有测试数据"""
        self.generate_tokenizer_data()
        self.generate_inference_data()
        self.generate_performance_data()
        self.generate_stress_data()

if __name__ == "__main__":
    generator = TestDataGenerator()
    generator.generate_all()
    generator.save("tests/data/test_cases.json")
    print("✅ 测试数据生成完成!")
```

---

## 🚀 执行流程

### 1. 准备阶段执行

```bash
#!/bin/bash
# scripts/prepare_tests.sh

echo "========================================="
echo "Phase 0: 测试准备阶段"
echo "========================================="

# P0.1: 下载 Qwen3 模型
echo "📥 下载 Qwen3 模型..."
./scripts/download_qwen3.sh &
PID_DOWNLOAD=$!

# P0.2: 准备测试数据
echo "📊 生成测试数据..."
python3 scripts/generate_test_data.py &
PID_DATA=$!

# P0.3: 配置测试环境
echo "⚙️  配置测试环境..."
export CLLM_TEST_MODEL_PATH="./test_models/qwen3"
export CLLM_TEST_DATA_PATH="./tests/data"
mkdir -p logs

# P0.4: 编译所有测试
echo "🔨 编译测试程序..."
cd build
cmake .. -DUSE_TOKENIZERS_CPP=ON -DBUILD_TESTS=ON
make -j8 all_tests
cd ..

# 等待并行任务完成
wait $PID_DOWNLOAD
echo "✅ 模型下载完成"

wait $PID_DATA
echo "✅ 测试数据准备完成"

# P0.5: 创建 Mock 对象（已经在代码中）
echo "✅ Mock 对象已就绪"

echo "========================================="
echo "✅ Phase 0 完成！"
echo "========================================="
```

---

### 2. 单元测试执行

```bash
#!/bin/bash
# scripts/run_unit_tests.sh

echo "========================================="
echo "Phase 1: 单元测试阶段"
echo "========================================="

# 定义测试列表
declare -A tests=(
    ["P1.1"]="test_http_server_unit"
    ["P1.2"]="test_hf_tokenizer_unit"
    ["P1.3"]="test_model_executor_unit"
    ["P1.4"]="test_libtorch_backend_unit"
    ["P1.5"]="test_qwen3_model_unit"
)

# 并行执行所有单元测试
run_test() {
    local phase=$1
    local test_name=$2
    
    echo "🧪 [$phase] 运行 $test_name..."
    ./build/bin/$test_name --gtest_output=json:logs/${test_name}.json \
        > logs/${test_name}.log 2>&1
    
    if [ $? -eq 0 ]; then
        echo "✅ [$phase] $test_name PASSED"
        return 0
    else
        echo "❌ [$phase] $test_name FAILED"
        return 1
    fi
}

export -f run_test

# 使用 parallel 并行执行
parallel -j 5 run_test ::: "${!tests[@]}" ::: "${tests[@]}"

# 汇总结果
echo "========================================="
echo "📊 Phase 1 测试结果汇总"
echo "========================================="

passed=0
failed=0

for phase in "${!tests[@]}"; do
    test_name="${tests[$phase]}"
    if grep -q '"failures": 0' logs/${test_name}.json 2>/dev/null; then
        echo "✅ [$phase] ${test_name}"
        ((passed++))
    else
        echo "❌ [$phase] ${test_name}"
        ((failed++))
    fi
done

echo "========================================="
echo "通过: $passed, 失败: $failed"
echo "========================================="
```

---

### 3. 集成测试执行

```bash
#!/bin/bash
# scripts/run_integration_tests.sh

echo "========================================="
echo "Phase 2: 模块集成测试阶段"
echo "========================================="

# Phase 2.1 和 2.3 可以并行（无依赖）
echo "🔄 并行执行 Phase 2.1 和 2.3..."
./build/bin/test_http_tokenizer_integration > logs/P2.1.log 2>&1 &
PID_21=$!

./build/bin/test_executor_backend_integration > logs/P2.3.log 2>&1 &
PID_23=$!

# 等待完成
wait $PID_21
echo "✅ Phase 2.1 完成"

wait $PID_23
echo "✅ Phase 2.3 完成"

# Phase 2.2 和 2.4 可以并行
echo "🔄 并行执行 Phase 2.2 和 2.4..."
./build/bin/test_tokenizer_executor_integration > logs/P2.2.log 2>&1 &
PID_22=$!

./build/bin/test_backend_qwen3_integration > logs/P2.4.log 2>&1 &
PID_24=$!

# 等待完成
wait $PID_22
echo "✅ Phase 2.2 完成"

wait $PID_24
echo "✅ Phase 2.4 完成"

echo "========================================="
echo "✅ Phase 2 完成！"
echo "========================================="
```

---

### 4. 完整执行脚本

```bash
#!/bin/bash
# scripts/run_all_tests.sh

set -e

echo "🚀 开始 cLLM 分阶段集成测试"
echo "========================================="

# 记录开始时间
START_TIME=$(date +%s)

# Phase 0: 准备
echo "📋 Phase 0: 准备阶段"
./scripts/prepare_tests.sh

# Phase 1: 单元测试
echo "📋 Phase 1: 单元测试阶段"
./scripts/run_unit_tests.sh

# Phase 2: 模块集成测试
echo "📋 Phase 2: 模块集成测试阶段"
./scripts/run_integration_tests.sh

# Phase 3: 子系统测试
echo "📋 Phase 3: 子系统测试阶段"
./scripts/run_subsystem_tests.sh

# Phase 4: 系统集成测试
echo "📋 Phase 4: 系统集成测试阶段"
./scripts/run_system_tests.sh

# Phase 5: E2E 场景测试
echo "📋 Phase 5: E2E 场景测试阶段"
./scripts/run_e2e_tests.sh

# 记录结束时间
END_TIME=$(date +%s)
DURATION=$((END_TIME - START_TIME))

echo "========================================="
echo "✅ 所有测试完成！"
echo "⏱️  总耗时: ${DURATION}秒 ($((DURATION / 60))分钟)"
echo "========================================="

# 生成测试报告
python3 scripts/generate_test_report.py
```

---

## 📈 监控和报告

### 测试进度监控

```python
# scripts/monitor_tests.py
import json
import os
from datetime import datetime

class TestMonitor:
    def __init__(self):
        self.results = {}
    
    def check_test_status(self, test_name):
        """检查测试状态"""
        log_file = f"logs/{test_name}.json"
        if not os.path.exists(log_file):
            return "pending"
        
        with open(log_file) as f:
            data = json.load(f)
            if data.get("failures", 0) == 0:
                return "passed"
            else:
                return "failed"
    
    def generate_progress_report(self):
        """生成进度报告"""
        report = {
            "timestamp": datetime.now().isoformat(),
            "phases": {}
        }
        
        # 检查每个阶段的进度
        for phase in ["P1", "P2", "P3", "P4", "P5"]:
            phase_tests = self.get_phase_tests(phase)
            total = len(phase_tests)
            passed = sum(1 for t in phase_tests 
                        if self.check_test_status(t) == "passed")
            failed = sum(1 for t in phase_tests 
                        if self.check_test_status(t) == "failed")
            pending = total - passed - failed
            
            report["phases"][phase] = {
                "total": total,
                "passed": passed,
                "failed": failed,
                "pending": pending,
                "progress": f"{passed}/{total}"
            }
        
        return report
    
    def save_report(self, filepath):
        """保存报告"""
        report = self.generate_progress_report()
        with open(filepath, 'w') as f:
            json.dump(report, f, indent=2)

if __name__ == "__main__":
    monitor = TestMonitor()
    monitor.save_report("logs/test_progress.json")
```

---

## 📝 总结

### 关键特性

1. **分阶段扩展**: 5 个阶段，从单元测试到 E2E 测试
2. **模块解耦**: 使用接口抽象和依赖注入
3. **并行化**: 最多 7 个 Agent 并行执行
4. **细粒度拆分**: 81 个独立任务
5. **完整覆盖**: HTTP、Tokenizer、Executor、Backend、Qwen3

### 执行时间估算

| 阶段 | 串行时间 | 并行时间（7 Agent）| 加速比 |
|------|---------|------------------|--------|
| Phase 0 | 3h | 1h | 3x |
| Phase 1 | 12h | 3h | 4x |
| Phase 2 | 14h | 4h | 3.5x |
| Phase 3 | 15h | 5h | 3x |
| Phase 4 | 24h | 8h | 3x |
| Phase 5 | 6h | 2h | 3x |
| **总计** | **74h** | **23h** | **3.2x** |

### 资源需求

- **计算资源**: 7 个并行 Agent（推荐 8 核 CPU + GPU）
- **存储空间**: ~50GB（模型 + 日志 + 数据）
- **测试时间**: 约 1 个工作日（23 小时）
- **人力投入**: 7 人并行或 1 人串行执行

---

**文档版本**: v1.0  
**创建时间**: 2026-01-11  
**下一次更新**: 执行后根据实际情况调整
